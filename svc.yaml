apiVersion: v1
kind: Service
metadata:
  name: rust-invest
  namespace: apps
spec:
  selector:
    app: rust-invest
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
